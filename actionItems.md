| ActionItem | Status | Severity | Summary | Description | Steps to Reproduce | Expected Result | Actual Result | Components | Fix | Comments |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **T-01** | Partial | Medium | GPU/Node Pinning | Pin pods to nodes .44 and .40 to ensure 1:1 GPU utilization. | Inspect `kubectl get pods -o wide` after deployment. | Pods pinned via `nodeSelector` to specific GPUs. | Dynamic scheduling without explicit pinning. | K8s | Pin workloads using `nodeSelector`. **Prompt:** "Update k8s/deployments.yaml to pin data-prep and triton to node .44 (slc6-lg-n3-b30-29) and pin scoring and model-train to node .40 (slc6-lg-n3-b30-25)." | Verified nodes are .44 and .40. |
| **T-02** | Partial | Medium | Scale Prep Pipes | Increase write pipes from 32 to 64 or 128 for FB pressure. | Run pipeline and monitor FlashBlade IOPS/Throughput. | 64-128 equivalent pipeline pressure. | Currently uses 32 write pipes. | Data-Prep | Tune BATCH_FILES and thread pool size in `gpu_worker.py`. **Prompt:** "Update k8s/deployments.yaml to add the WRITE_PIPES environment variable set to '128' and change BATCH_FILES to '20' for the data-prep container." | - |
| **T-03** | Gap | High | 200ms GPU Polling | Align DCGM, Prometheus, and Backend to 200ms resolution. | Observe dashboard GPU charts during 1-2s kernel bursts. | Real-time burst visibility in UI. | 15s stale data due to ServiceMonitor interval. | K8s, Prometheus, DCGM | **Prompt:** "Update k8s/dcgm-servicemonitor.yaml to set interval: 200ms and update monitoring/prometheus.yml to set scrape_interval: 200ms for the dcgm job. Ensure DCGM_EXPORTER_COLLECT_INTERVAL_MS is set to 200 in the exporter config." | Verified current config is 15s/1s mismatch. |
| **T-05** | Partial | Low | Responsive Dashboard | Update Business tab for mobile, 1080p, and 4K scaling. | Open dashboard on different screen resolutions. | Balanced, readable layout on all displays. | Hardcoded layout issues on non-1080p screens. | Dashboard | Apply CSS Flexbox/Grid updates to `dashboard.html`. | - |
| **T-06** | Gap | Medium | Full State Reset | Clear all counters and model artifacts on reset. | Run demo, click Reset, and check SHAP/Summary tabs. | Clean-slate UI and backend state. | Stale files (SHAP/summaries) remain after reset. | Backend, Dashboard | **Prompt:** "Update metrics.py and backend.py to clear the state.total_rows_processed counter and delete shap_summary.json during the reset_pipeline command." | - |
| **T-08** | Gap | High | End-to-End Throughput | Replace prep-only RPS with measured end-to-end TPS. | Compare `prep_rows_per_sec` against total runtime. | Accurate metrics from raw data to score output. | KPI uses isolated prep-stage kernel time only. | Backend | **Prompt:** "Update metrics.py to calculate a true end-to-end TPS by dividing the row count by the time elapsed since the chunk was first created (using chunk_ts), replacing the isolated prep-stage kernel rate." | Critical for customer credibility. |
| **T-09** | Gap | Critical | Cross-node EPERM | Resolve 'Operation not permitted' between nodes .40 and .44. | Deploy scoring to .40 and Triton to .44; check logs. | Stable cross-node gRPC communication. | `EPERM` error due to proxy interception. | K8s, Scoring, Triton | Switch to gRPC and add NO_PROXY. **Prompt:** "Update the scoring deployment in k8s/deployments.yaml to use TRITON_URL=triton:8001 and add NO_PROXY='triton,localhost,127.0.0.1,.svc.cluster.local' to the environment." | Root cause is `http_proxy` environment variables. |
| **D-01** | Gap | High | Real Data KPIs | Replace synthetic estimates with Parquet-derived aggregation. | Inspect `metrics.py` for hardcoded average amounts ($250). | Data-driven metrics reflecting actual model results. | Dashboard uses synthetic fallbacks/estimates. | Backend | Implement real aggregation in `metrics.py` using `pandas` on `*.parquet` scores. **Prompt:** "Update metrics.py _compute_kpis to dynamically calculate fraud_exposure_usd by summing the 'amt' column of fraudulent rows from the recent /data/scores parquet files instead of using the hardcoded avg_fraud_amt of 250.0." | Verified: Fraud Exposure is hardcoded to $250 avg. Alerts and Infra KPIs are real. |
| **App Testing**| Completed | High | Peak Load Stress | Ensure application stability under peak data generation. | Scale `data-gather` to 10+ replicas. | No pod crashes or I/O stalls under load. | Verified: 4 active pods (GPU limit) saturated all 4 L40S GPUs and wrote to FlashBlade at ~450k rows/sec aggregate without stalls. | All | Run large-scale synthetic data generation via `data-gather`. | Limited to 4 replicas due to physical GPU availability (4x L40S). |
| **Prep Validation** | Completed | Low | Sequential Processing | Verify files are processed in chronological order. | Audit pod logs for file processing sequence. | Sequential processing of raw data batches. | Verified: Alpha/numeric sort matches timestamps. | Data-Prep | Already verified via file-queue logic. | - |
| **Alerts Audit** | Completed | Medium | Alerts Table Integrity | Confirm alerts read directly from scored Parquets. | Audit WebSocket stream logic in `metrics.py`. | Real-time stream from disk. | Verified: Correctly reading from `/data/scores/`. | Backend, Dashboard | No fix needed. | - |
| **Scoring Review**| In-Progress | Medium | Scoring Logic Audit | Understand relational fraud scoring logic. | Review `WindowedGraph` and ensemble code in `scorer.py`. | Full understanding of GNN+XGBoost logic. | Ongoing research. | Scoring | TBD | - |
| **D-02** | Pending | Medium | Remove Fake Weighting | Remove hardcoded 3x multipliers in frontend. | Inspect `dashboard.html` `catData` weighting. | Raw model outputs visualized accurately. | 3x weighting used for travel/shopping categories. | Dashboard | **Prompt:** "Update dashboard.html to remove the hardcoded catData weighting and use the aggregated fraud_by_category dictionary from the WebSocket message." | Verified: dashboard.html applies 3x fake multiplier to shopping_net, travel, misc_net. |
| **D-03** | Pending | High | Global Counter Bug | Correctly initialize total rows processed on startup. | Restart backend pod and check total transaction count. | Accurate cumulative totals across restarts. | Counter resets to 0 on backend restart. | Backend | **Prompt:** "Update metrics.py to scan /data/raw/*.done files on startup to initialize total_rows_processed and use real amt sums from /data/scores for exposure." | Requires scanning `*.done` files. |
