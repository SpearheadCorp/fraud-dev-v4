| ID | Requirement | Target Expectation | Current State | Status | Customer Impact | Recommended Action | Manual Validation |
|:---|:---|:---|:---|:---|:---|:---|:---|
| **T-01** | Use all 4 GPUs with dedicated functions | One clear workload per GPU across inference, feature engineering, scoring, and inline training | The pipeline starts 4 GPU pods and is designed around 4 roles, but only Triton is explicitly pinned to a node. Other GPU pods rely on scheduler placement instead of fully enforced node/GPU assignment. | Partial | The intended "one workload per GPU" story may hold in practice, but it is not fully guaranteed by manifests alone. | Pin all 4 workload pods to explicit nodes and validate placement per demo environment. | Start the pipeline and confirm each pod lands on the intended node and each of the 4 GPUs shows the expected workload. |
| **T-02** | Increase load to 64-128 pipes | Enough parallelism to saturate GPUs and make FlashBlade headroom visible | The implementation uses mega-batch processing and 32 write pipes, but current read/write/batch settings do not clearly match the BRD's 64-128 pipe target. | Partial | The demo may show strong bursts, but the field story is weaker if the documented load target is not visibly achieved. | Define the approved demo load profile and document the exact settings used to claim 64-128 equivalent pipeline pressure. | Run the demo under the chosen load profile and capture GPU utilization plus FlashBlade latency for that configuration. |
| **T-03** | Sub-second GPU polling | Dashboard should capture short GPU bursts; BRD recommends sub-100ms visibility | The browser updates every 200ms, but the underlying DCGM metric used is still a 1-second profiling metric. | Gap | Short GPU bursts may still be under-represented, making saturation appear less convincing. | Use a faster GPU telemetry source or explicitly document the effective resolution limit of the dashboard. | Compare dashboard GPU traces with a finer-grained source during a live batch and confirm burst visibility. |
| **T-04** | Inline training active during demo | Training must run continuously alongside scoring, not only offline | Continuous model-train is implemented and writes updated training artifacts during the live run. | Met | This supports the core v4 story well. | Keep, and make the live update cadence more visible in the UI. | Start the pipeline and confirm new training cycles occur while scoring continues without stalling. |
| **T-05** | Responsive dashboard layout | UI should scale cleanly across demo resolutions | The dashboard uses responsive grid behavior, but it has not been fully validated across the range of likely customer display setups. | Partial | A cluttered or awkward layout on a meeting-room display would weaken the demo. | Validate and tune layout on laptop, 1080p, and 4K screens. | Resize the dashboard across target resolutions and confirm all tabs remain readable and balanced. |
| **T-06** | Complete start/stop/reset state zeroing | No residual counters, charts, or model state after reset | Start/stop/reset flows are materially improved, but reset preserves model metrics artifacts such as SHAP/training summaries, which can reappear after reset. | Gap | This creates risk during back-to-back demos because stale model-quality results may appear as if they belong to the new run. | Extend reset behavior to clear or intentionally version model-view state, and define whether model metrics are run-specific or persistent. | Run a full demo, reset, reopen the Model tab, and confirm no stale run data is shown. |
| **T-07** | Triton startup progress messaging | 60-90 second startup is acceptable if progress is clear | The dashboard shows pod startup progress and a modal during transitions. | Met | Facilitators have a clearer wait-state story. | Keep as-is; optionally add stage-specific wording for Triton model load. | Trigger a cold start and confirm operators can tell whether the system is still progressing normally. |
| **T-08** | TPS validation | Replace estimates with measured benchmark numbers before customer-facing use | The dashboard surfaces throughput, but current KPI logic is derived from prep-stage feature time rather than a formal end-to-end benchmark. | Gap | Customer-facing TPS claims would not yet be defensible. | Run a controlled benchmark and publish measured end-to-end throughput figures with test conditions. | Execute the benchmark runbook and confirm reported TPS matches collected logs and output counts. |
| **T-09** | Realistic transaction amounts | Categories such as grocery should not show unrealistic values | Category-based amount caps are implemented in data generation, including lower caps for grocery. | Met | This fixes a visible credibility issue from the earlier demo. | Keep; validate sampled outputs periodically. | Inspect recent scored transactions by category and confirm amounts stay within realistic ranges. |
| **D-01** | Business tab should contain only business-facing metrics | Volume, rate, exposure, fraud, category, geography, alerts | The business tab is cleaner than before, but it is still incomplete: no fraud-by-geography view, no dollar value processed KPI, and category values are currently estimated rather than derived from actual scored data. | Gap | The primary executive view is not yet fully trustworthy or complete. | Replace synthetic/estimated widgets with output-derived metrics and add geography and dollar-value processed. | Compare business-tab values against direct aggregation from score files for the same run. |
| **D-02** | Dedicated technical views | Separate business, model, and pipeline/technical views | The current dashboard provides Business, Infrastructure, and Model / SHAP, which is close to the March 17 email direction, but not fully aligned with the BRD request for dedicated model and pipeline technical views. | Partial | The story is clearer than v3, but some technical content is still spread differently than requested. | Decide on the final stakeholder-approved tab model, then align labels and contents consistently. | Review the tab structure with stakeholders and confirm each audience can find its metrics without confusion. |
| **D-03** | Model tab content | Precision/recall, training metrics, SHAP, model story | The model tab includes KPI cards and SHAP, but lacks precision/recall curves, training loss curves, and model update frequency visualization. | Partial | Technical viewers do not yet get the full model-behavior story requested in the BRD. | Add trend views for precision/recall, loss, and model refresh cadence. | Confirm the Model tab updates over time and reflects multiple training cycles during a live run. |
| **D-04** | Pipeline tab content | Stage flow, throughput indicators, GPU utilization, inference latency distribution | There is no dedicated pipeline tab today. Related metrics are split across Infrastructure and Model, and there is no inference latency distribution chart. | Gap | The technical story is harder to present cleanly to field SEs. | Add a dedicated pipeline/technical tab or explicitly re-baseline the requirement with stakeholders. | Walk through the pipeline story end-to-end and confirm a presenter can explain stage flow from one view. |
| **D-05** | FlashBlade narrative clarity | Show GPU saturation increasing while FlashBlade latency stays flat | The current implementation does show GPU activity and FlashBlade latency together, which supports the intended narrative. | Met | This is one of the strongest parts of the current demo. | Preserve this pairing and use a validated run profile during demos. | Run under target load and confirm GPU activity climbs while FlashBlade latency remains steady. |
| **UX-01** | Controls should be intuitive during transitions | In-progress indicators and proactive messaging | The dashboard includes transition modals and progress states for startup, stop, and reset. | Met | Facilitators have a cleaner experience and fewer ambiguous wait states. | Keep; minor wording polish only if desired. | Exercise start, stop, and reset repeatedly and confirm the modal states remain accurate. |
| **UX-02** | Back-to-back demo readiness | Operators should be able to rerun the demo cleanly | Raw data re-queue and downstream cleanup are implemented, but stale model-view artifacts remain a risk after reset. | Partial | Most of the pipeline resets cleanly, but the overall demo is not yet fully clean-room between runs. | Expand reset scope or distinguish persistent model assets from run-specific UI artifacts. | Complete one run, reset, restart, and verify that no data from the prior run is visible. |
